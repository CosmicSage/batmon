#include <iostream>
#include <windows.h>

int main(int argc, char const *argv[]) {
  while(1) {
    // std::cout << "Batman" << '\n';
    Beep(1000, 500);
    // Beep(523, 500);
  }
  return 0;
}
